---
driver:
  name: vagrant

provisioner:
  name: shell

platforms:
  - name: vbox/bionic64
    driver:
      box: bionic64
      provider: virtualbox

verifier:
  name: inspec

suites:
  - name: default
